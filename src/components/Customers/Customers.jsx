import React from "react";

const Customers = ({ inViewRef }) => {
  return (
    <section className="section" id="customers" ref={inViewRef}>
       <h1>Հաճախորդներ</h1>
    </section>
   )
};

export default Customers;
